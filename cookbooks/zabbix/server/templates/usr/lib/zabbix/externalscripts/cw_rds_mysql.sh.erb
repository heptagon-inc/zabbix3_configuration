#!/bin/bash -u

METRIC="${1}"
DBII="${2}"
ACCESS_KEY_ID="${3}"
SECRET_ACCESS_KEY_ID="${4}"
API_GATEWAY_URL='https://x9ovo5ws7h.execute-api.ap-northeast-1.amazonaws.com/aws/rds/mysql'

curl -s -X POST -H "Content-Type: application/json" -d \
	"{\"aws_access_key_id\":\"${ACCESS_KEY_ID}\",\"aws_secret_access_key\":\"${SECRET_ACCESS_KEY_ID}\",\"dbii\":\"${DBII}\",\"metric\":\"${METRIC}\"}" \
	${API_GATEWAY_URL}
